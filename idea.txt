**AI Coding session logs: https://opncd.ai/share/c1gExpME**
Below is the text file uploaded to the llm for providing my approach as well as the crux of the project and what are its demands.

SYSTEM ROLE:
You are an AI system that builds a graph-based enterprise document flow engine using Neo4j and a hybrid RAG pipeline.

OBJECTIVE:
Transform SAP-style transactional datasets into a traceable graph and enable natural language querying over document flows (Order → Delivery → Billing → Payment).

---

STEP 1: DATA INGESTION

* Parse all dataset folders:
  sales_order_*, outbound_delivery_*, billing_document_*, journal_entry_*, payments_*, business_partner_*, products_*, plants

* Normalize into entities:

  Core:
  SalesOrder
  SalesOrderItem
  Delivery
  DeliveryItem
  BillingDocument
  BillingItem
  JournalEntry
  Payment

  Supporting:
  Customer
  Address
  Product
  Plant

---

STEP 2: GRAPH CONSTRUCTION (Neo4j)

Create nodes and relationships:

(Customer)-[:PLACED]->(SalesOrder)

(SalesOrder)-[:HAS_ITEM]->(SalesOrderItem)

(SalesOrderItem)-[:MAPS_TO]->(DeliveryItem)

(DeliveryItem)-[:PART_OF]->(Delivery)

(Delivery)-[:BILLED_AS]->(BillingDocument)

(BillingDocument)-[:RECORDED_AS]->(JournalEntry)

(JournalEntry)-[:CLEARED_BY]->(Payment)

(SalesOrderItem)-[:CONTAINS]->(Product)

(Product)-[:LOCATED_AT]->(Plant)

(Customer)-[:HAS_ADDRESS]->(Address)

---

STEP 3: GRAPH INDEXING

* Create indexes on:
  SalesOrder.salesOrder
  Delivery.deliveryDocument
  BillingDocument.billingDocument
  Customer.businessPartner
  Product.product

---

STEP 4: RAG LAYER

* Create text chunks describing:

  * Entity summaries
  * Relationship chains

* Example:
  "SalesOrder 740506 → Delivery 80737721 → Billing 90504274 → Payment 9400000220"

* Store embeddings in vector DB

---

STEP 5: QUERY PIPELINE

For each user query:

1. PROMPT ENHANCEMENT
   Convert natural language into structured intent

2. QUERY CLASSIFICATION

   * Flow tracing → Graph traversal
   * Aggregation → Cypher query
   * Contextual → RAG

3. QUERY EXECUTION

   * Use Neo4j (Cypher) for structured queries
   * Use vector DB for semantic lookup
   * Combine results if needed

4. RESPONSE GENERATION

   * Use only retrieved data
   * Include entity IDs in response

---

STEP 6: FLOW ENGINE (CORE FEATURE)

Support:

* Full trace:
  SalesOrder → Delivery → Billing → Payment

* Broken flow detection:

  * Delivered but not billed
  * Billed but not paid

---

STEP 7: GUARDRAILS

* Reject unrelated queries
* Respond:
  "This system only answers questions about the provided business dataset"

---

STEP 8: OUTPUT FORMAT

* Natural language answer
* Include:

  * Entity IDs
  * Flow paths

---

CONSTRAINTS:

* Do NOT hallucinate
* Prefer graph queries over LLM reasoning
* Use RAG only when graph lacks context
